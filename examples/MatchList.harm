module MatchList

import IO.Console as Console

fun length(list: [int]) -> int =
    case list of
        | [] => 0
        | [_, ..xs] => length (xs) + 1
    end

fun main =
    Console.println (length ([1, 2, 3, 4]))